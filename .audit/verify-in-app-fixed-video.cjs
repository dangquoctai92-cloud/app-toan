const fs=require('fs'),http=require('http');const snippet=`<script>const report=(type,data)=>fetch('/report',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type,data,at:Date.now()})});report('app-open',{ua:navigator.userAgent});window.addEventListener('error',e=>report('app-error',{message:e.message}));startLesson('b1');setTimeout(()=>startLessonVideo(),500);for(const ms of [1000,9000,20000])setTimeout(()=>report('app-video',{state:document.querySelector('.video-wrap').dataset.state,cover:!document.getElementById('video-cover').classList.contains('hidden'),iframe:!!document.getElementById('ytFrame'),status:document.getElementById('video-status').textContent,practiceEnabled:!document.getElementById('video-start-btn').disabled}),ms);</script>`;http.createServer((req,res)=>{if(req.method==='POST'&&req.url==='/report'){let text='';req.on('data',d=>text+=d);req.on('end',()=>{JSON.parse(text);fs.appendFileSync('.audit/in-app-fixed-video.jsonl',text+'\n');res.end('ok')});return}res.setHeader('Content-Type','text/html; charset=utf-8');res.setHeader('Cache-Control','no-store');res.end(fs.readFileSync('index.html','utf8').replace('</body>',snippet+'</body>'))}).listen(8781,'127.0.0.1',()=>console.log('App verification ready 8781'));
