const fs=require('fs');(async()=>{const a=JSON.parse(fs.readFileSync('.audit/video-selection.json'));const out=[];for(const row of a.filter(x=>!x.selected)){const id=row.attempts[0].id;try{const r=await fetch('https://www.youtube.com/oembed?url='+encodeURIComponent('https://www.youtube.com/watch?v='+id)+'&format=json',{signal:AbortSignal.timeout(12000)}),v=r.ok?await r.json():{};out.push({lesson:row.lesson,id,status:r.status,title:v.title,channel:v.author_name});}catch(e){out.push({lesson:row.lesson,id,error:e.message})}}fs.writeFileSync('.audit/video-public-check.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));})();
