const fs=require('fs');fs.mkdirSync('.audit/web-test-dist',{recursive:true});fs.copyFileSync('index.html','.audit/web-test-dist/index.html');fs.writeFileSync('.audit/web-test-review.md','# Bản web test chờ xác nhận\n\nTệp dự kiến đăng: chỉ `web-test-dist/index.html` (bản app đã tạo từ nguồn), gồm 81 bài và danh mục video. Không đăng thư mục sách, ảnh người dùng gửi, nhật ký kiểm tra, bản sao dự phòng hoặc tiến độ trong trình duyệt. Chưa tạo site và chưa tải tệp lên dịch vụ nào.\n\nMục đích: kiểm thử app trên địa chỉ web thật, trong chế độ xem web được hỗ trợ, với video YouTube nhúng. Chưa khẳng định bản web sẽ phát thành công trong Codex cho đến khi kiểm chứng trực tiếp.\n');fs.appendFileSync('KET-QUA-SUA-APP.md','\n## Xác định lỗi màn hình đen trong Codex — 14/09/2026\n\n- Đã tái hiện trên chính trình duyệt nhúng Chrome 152 của Codex qua trang kiểm tra cục bộ. Thư viện YouTube tải xong; iframe YouTube cả hai miền không hoàn thành tải, không nhận ready/state. Iframe cùng máy tải bình thường.\n- Đối chiếu mã trình duyệt đã cài, phiên bản 26.908.4834.0: hàm Yie kiểm tra subFrame từ URL cục bộ sang URL ngoài; bộ xử lý request hủy trường hợp này khi không ở chế độ agent điều khiển. Đây là giới hạn của khung xem trước, không phải bằng chứng video sai hoặc thiếu codec. Không sửa, vô hiệu hóa hoặc tìm cách vượt quy tắc này.\n- Sửa app: theo dõi sự kiện iframe load riêng với trạng thái thư viện; sau 8 giây iframe chưa tải thì dừng/gỡ khung, khôi phục ảnh và nút thử lại. Nếu iframe đã tải nhưng thiếu thư viện điều khiển, tiếp tục cho bấm trực tiếp, không phủ lên video.\n- Xác minh đúng môi trường bằng `.audit/in-app-fixed-video.jsonl`: tại 9 và 20 giây, trạng thái error rõ ràng, ảnh hiện, iframe đã gỡ, nút luyện tập vẫn dùng được. Điều này xác nhận sửa trạng thái treo, không khẳng định video đã phát được trong localhost của Codex.\n- Kiểm tra mô phỏng 81 bài, tải chậm thư viện, chặn tự phát, iframe bị chặn, phản hồi lỗi đến muộn, 20 trường hợp giao diện và bản tạo đều qua.\n- Muốn nghiệm thu video nhúng ngay trong khung Codex cần môi trường web được hỗ trợ. Bản chỉ gồm index.html đã chuẩn bị tại `.audit/web-test-dist/`; đang chờ người dùng chấp thuận đăng bản test. Chưa đăng app.\n');let readme=fs.readFileSync('README.md','utf8');readme=readme.replace('Mở `index.html` để dùng app.','Dùng máy chủ cục bộ và mở app trong trình duyệt đầy đủ để xem video YouTube; mở trực tiếp `index.html` có thể bị YouTube từ chối do thiếu nguồn giới thiệu.');readme+='\n## Giới hạn khung xem trước Codex\n\nỞ phiên bản đã kiểm tra (26.908.4834.0), chế độ xem localhost chặn iframe từ Internet. Vì vậy kiểm tra phát thành công trên Edge không đủ để kết luận sẽ phát ngay trong khung xem trước. App xử lý khung bị chặn bằng ảnh bài giảng, thông báo và nút thử lại sau 8 giây, thay cho khung đen vô hạn. Không thay đổi cơ chế bảo vệ của Codex. Bản web test cần được đăng và kiểm chứng riêng nếu người dùng muốn test video ngay trong khung này.\n';fs.writeFileSync('README.md',readme);console.log('Local fix saved; web-test package prepared, not published.');
