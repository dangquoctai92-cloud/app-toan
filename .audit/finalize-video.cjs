const fs=require('fs');const path='src/interface.css';let css=fs.readFileSync(path,'utf8');css=css.replace('.video-wrap{position:relative;aspect-ratio:16/9;min-height:210px;', '.video-wrap{position:relative;width:100%;min-width:0;aspect-ratio:16/9;min-height:210px;');fs.writeFileSync(path,css);
const catalog=JSON.parse(fs.readFileSync('src/video-catalog.json'));const checks=[...JSON.parse(fs.readFileSync('.audit/video-live-all.json')),...JSON.parse(fs.readFileSync('.audit/video-live-retry.json'))];for(const [code,meta]of Object.entries(catalog)){const check=checks.findLast(x=>x.code===code&&['ready','playing'].includes(x.state)&&x.id===meta.id);if(!check)throw Error('Unverified '+code);meta.durationSeconds=check.duration;meta.embedAllowed=true;meta.verification='source-oembed-and-embed-ready';meta.embedCheckedAt=new Date().toISOString();}fs.writeFileSync('src/video-catalog.json',JSON.stringify(catalog,null,2)+'\n');console.log('81 embeds verified; video width constrained.');
