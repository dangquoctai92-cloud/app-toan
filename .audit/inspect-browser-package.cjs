const fs=require('fs');const file='C:/Program Files/WindowsApps/OpenAI.Codex_26.908.4834.0_x64__2p2nqsd0c76g0/app/resources/app.asar';const fd=fs.openSync(file,'r'),buf=Buffer.alloc(16);fs.readSync(fd,buf,0,16,0);const len=buf.readUInt32LE(12),h=Buffer.alloc(len);fs.readSync(fd,h,0,len,16);const header=JSON.parse(h.toString());const names=[];function walk(x,p=''){for(const[k,v]of Object.entries(x.files||{})){const n=p+k;if(v.files)walk(v,n+'/');else if(/\.js$/.test(n)&&(/browser|preload|main/.test(n)))names.push({name:n,size:v.size,offset:v.offset})}}walk(header);console.log(JSON.stringify({base:8+buf.readUInt32LE(4),files:names.filter(x=>!x.name.includes('node_modules')).slice(0,65)},null,2));fs.closeSync(fd);
